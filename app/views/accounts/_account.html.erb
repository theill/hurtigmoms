<tr id="<%= dom_id(account) %>" class="account row <%= account_type_class(account) %>">
	<td class="account-no actions"><div class="buttons-container"><div class="buttons"><%= link_to(image_tag('navigation-000-frame.png'), edit_account_path(account), :class => 'edit hide') %></div></div><%=h '%05d' % account.account_no %></td>
	<td class="name"><%= account.name %></td>
	<td><%= type_of_vat(account) %></td>
</tr>